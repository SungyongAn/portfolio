# ============================================================
# backend/requirements.txt
# Python 3.12 / FastAPI バックエンド 依存パッケージ
# ============================================================

# ── Web フレームワーク ──────────────────────────────────────
fastapi==0.115.12
uvicorn[standard]==0.34.0          # ASGI サーバー (uvloop + httptools)

# ── DB / ORM / マイグレーション ────────────────────────────
sqlalchemy==2.0.40
alembic==1.14.1
pymysql==1.1.1                     # MySQL ドライバ（純 Python）
cryptography==44.0.2               # PyMySQL の SSL 対応用

# ── 認証・セキュリティ ─────────────────────────────────────
argon2-cffi==23.1.0                # パスワードハッシュ (Argon2id)
python-jose[cryptography]==3.3.0  # JWT 生成・検証
passlib==1.7.4                     # パスワードユーティリティ補助

# ── HTTP / Cookie ──────────────────────────────────────────
python-multipart==0.0.20           # フォームデータ / ファイルアップロード

# ── メール送信 ─────────────────────────────────────────────
fastapi-mail==1.4.2                # 予約通知・リマインドメール送信

# ── バリデーション ─────────────────────────────────────────
pydantic[email]==2.11.3            # FastAPI のスキーマ検証 / email-validator 内包
pydantic-settings==2.8.1           # .env 読み込み (BaseSettings)

# ── 日時・スケジューラ ─────────────────────────────────────
python-dateutil==2.9.0.post0       # 祝日考慮の日付計算補助
apscheduler==3.11.0                # 定期実行ジョブ（リマインド・予約期限失効）
jpholiday==0.1.9                   # 日本祝日判定（金曜締め切り処理）

# ── CSV 出力 ───────────────────────────────────────────────
# 標準 csv モジュールを使用するため追加パッケージなし

# ── ロギング ──────────────────────────────────────────────
structlog==25.3.0                  # 構造化ログ

# ── 開発・テスト (本番イメージには含めないこと) ────────────
# httpx==0.28.1                    # FastAPI TestClient 用
# pytest==8.3.5
# pytest-asyncio==0.25.3
# coverage==7.8.0