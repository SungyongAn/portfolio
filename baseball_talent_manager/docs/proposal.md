# 課題2 提案内容

## 1. 機能改善提案

### 1.1 重複登録防止
**課題：**
複数のマネージャーが同じ部員・同じ計測日の測定記録を同時に入力した場合、重複登録が発生する可能性がある。

**提案：**
- `create_measurement()` に同一部員・同一計測日の重複チェックを追加
- 重複が検出された場合は400エラーを返す

**実装箇所：**
- `backend/app/services/measurement_service.py`

---

### 1.2 チーム全体の傾向可視化ダッシュボード
**課題：**
現状、測定記録はテーブル形式での閲覧のみで、チーム全体の傾向把握が困難。

**提案：**
EChartsを使った可視化ダッシュボードを監督・コーチ向けに追加する。

**具体的な可視化内容：**
- 項目別チーム平均の時系列推移（折れ線グラフ）
- 部員個人の測定結果時系列比較（折れ線グラフ）
- 部員同士の能力比較（レーダーチャート）
- チーム内での自分の位置づけ（部員向け：偏差値・順位表示）

**実装箇所：**
- `frontend/src/views/shared/DashboardView.vue`（グラフ追加）
- `frontend/src/components/MeasurementChart.vue`（新規作成）

---

### 1.3 マネージャーの入力履歴管理
**課題：**
現状、測定記録に入力者の情報が記録されないため、入力ミス発生時の追跡が困難。

**提案：**
- `measurements` テーブルに `created_by`（入力者のuser_id）カラムを追加
- 測定記録登録時に入力者のuser_idを記録

**実装箇所：**
- `backend/alembic/versions/` （マイグレーション追加）
- `backend/app/models/measurement.py`
- `backend/app/services/measurement_service.py`

---

### 1.4 部員・コーチ間のメッセージ機能
**課題：**
現状、部員の希望やコーチからのフィードバックを記録する手段がない。

**提案：**
- 部員ごとにコーチ・監督からのメッセージと部員自身の目標を記録できる機能を追加
- 測定記録閲覧画面にメッセージ欄を追加

**実装箇所：**
- `backend/alembic/versions/` （新規テーブル追加）
- `backend/app/models/` （新規モデル追加）
- `frontend/src/components/` （新規コンポーネント追加）

---

### 1.5 測定項目の柔軟化
**課題：**
現状、測定項目が固定されており、項目の追加・変更に対応できない。また部員からは「50m走以外の計測手法も試してほしい」という要望がある。

**提案：**
- 測定項目をマスタテーブルで管理し、追加・変更を可能にする
- 項目ごとに有効・無効を切り替えられる設計にする

**実装箇所：**
- `backend/alembic/versions/` （マスタテーブル追加）
- `backend/app/models/` （新規モデル追加）

---

### 1.6 一括処理機能
**課題：**
現状、測定記録の入力・引退処理が1件ずつの対応のみで、マネージャーの操作負担が大きい。

**提案：**
- 複数部員の測定記録を1画面で一括入力できる機能
- 3年生の一括引退処理機能

**実装箇所：**
- `frontend/src/views/manager/MeasurementResultSubmit.vue`
- `frontend/src/components/MemberRetire.vue`

---

### 1.7 スマートフォン対応
**課題：**
現状、スマートフォンでの操作・閲覧に最適化されていない。

**提案：**
- レスポンシブデザインの改善
- モバイル向けUIの最適化（テーブルのスクロール対応など）

**実装箇所：**
- 各コンポーネントのCSS改善


### 1.8 測定結果閲覧画面の改善
**課題：**
現状、測定記録はテーブル形式での閲覧のみで、データ量が増えると目的のデータを探しにくい。
また将来的にポジション管理など属性が増えた際に絞り込みが必要になる。

**提案：**
- 学年・部員名・計測日による絞り込み機能
- ソート機能（全カラム対応）
- ページネーション機能（表示件数切り替え）
- フィルタ・ソート条件のURL同期（ブラウザバック対応）

**実装箇所：**
- `frontend/src/components/MeasurementResultList.vue`

**実装状況：実装済み**

---

## 2. 運用ルール整備提案

### 2.1 測定記録の入力ルール
- 測定記録の入力は測定日当日または翌日までに行う
- 入力後は速やかに承認フローを発行する
- 入力ミスが発覚した場合はマネージャーが作り直す（否認→再作成）

### 2.2 承認フローの運用ルール
- 部員は承認依頼を受けたら**2営業日以内**に承認・否認を行う
- コーチは部員承認後**2営業日以内**に最終承認・否認を行う
- 否認の場合はマネージャーに理由を口頭で伝える

### 2.3 アカウント管理ルール
- マネージャーは個別アカウントを使用する（共有アカウント禁止）
- パスワードは定期的に変更する
- 卒業・退部時はコーチ・監督がアカウントのステータスを更新する

### 2.4 測定日の運用ルール
- 測定は2か月に1度、全部員参加で実施する
- 欠席者の測定は別日に実施し、同じ計測日で登録する
- 同一部員・同一計測日の重複登録は禁止する

---

## 3. 実装優先度

| 提案 | 優先度 | 理由 | 実装状況 |
|------|--------|------|--------|
| 1.1 重複登録防止 | 高 | データ品質に直結 | 未実装 |
| 1.2 傾向可視化 | 高 | 課題2のメイン要件 | 未実装 |
| 1.8 測定結果閲覧画面の改善 | 高 | データ増加時の利便性向上・将来的な拡張への対応 | 実装済み |
| 1.3 入力履歴管理 | 中 | 運用上重要だが緊急度は低い | 未実装 |
| 1.6 一括処理 | 中 | マネージャーの負担軽減 | 未実装 |
| 1.4 メッセージ機能 | 中 | 現場からの要望あり | 未実装 |
| 1.7 スマホ対応 | 中 | 部員からの要望あり | 未実装 |
| 1.5 測定項目柔軟化 | 低 | 将来的な拡張として検討 | 未実装 |