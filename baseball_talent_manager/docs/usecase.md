
# ユースケース一覧

## 1. 共通仕様

### ■ 課題1

### ヘッダー表示
- 全ロール共通のヘッダーにユーザー名・ロール・ログアウトボタンを表示する
- ログアウトボタンはヘッダーから常時アクセス可能

### ログイン
- **事前条件**：特になし
- **基本フロー**：メールアドレス・パスワードで認証
- **備考**：ログインユーザーのロールに合わせて表示画面の振り分けを行う

### ログアウト
- **事前条件**：ログイン済み
- **基本フロー**：ログアウトボタンを押すとセッションを破棄しログイン画面へ遷移
- **備考**：リフレッシュトークン（HttpOnly Cookie）を削除する

---

## 2. マネージャー

---

### ■ 課題1

### 部員一覧の参照
- **事前条件**：測定結果入力時に部員一覧を参照可能
- **備考**：部員の作成・退部・引退は不可。参照のみ。

### 測定結果の入力
- **事前条件**：マネージャーのみアクセス可能
- **基本フロー**：
  1. 測定日の入力（カレンダーから選択 or 手入力）
  2. 部員１名を選択
  3. 各測定項目を入力
     - 走力（2種）：50m走（sec）/ ベースランニング（sec）
     - 肩力（2種）：遠投（m）/ ストレート球速（km/h）
     - 打力（2種）：打球速度（km/h）/ スイング速度（km/h）
     - 筋力（2種）：ベンチプレス（kg）/ スクワット（kg）
  4. 保存
  4. 承認依頼を発行

### 否認後の再作成
- **事前条件**：否認された測定結果が存在
- **基本フロー**：
  1. 否認ステータス確認
  2. 既存データの編集ではなく、新規レコードとして作成する
  3. 保存
  4. 承認依頼発行
- **備考**：
  - 否認されたレコードの直接編集は不可
  - 再作成は必ず新規レコードとして保存する
  - 否認理由の記録は今回のPoCでは対象外とする

  ### 全部員の測定記録の閲覧
- **基本フロー**：
  1. 測定記録の閲覧専用ページを使用
  2. 承認済みの全測定記録を一覧で確認する
- **備考**：値の修正は不可

### 課題２

### 測定記録の検索・ソート
- 名前・学年・項目名での検索機能
- 各項目でのソート機能

### 測定記録のグラフ表示
- **事前条件**：承認済みの記録が1件以上存在する
- **基本フロー**：
  1. グラフ表示専用ページを使用
  2. 表示項目（項目混合・項目別など）を任意で選択しグラフを表示する
- **備考**：
  - 表示方法は項目別のみ（項目混合は対象外）
  - 表示方法の詳細はUI設計時に要検討


### 複数部員一括保存
- 複数部員を1画面で入力
- ページネーション表示（10人単位）
- 送信済／未送信管理UI
- マネージャー操作負荷軽減UI改善


---

## 3. 部員

---

### ■ 課題1

### 測定結果の確認
- **事前条件**：確認依頼が届いている
- **基本フロー**：
  1. 測定結果確認ページを開く
  2. 値を確認
  3. 承認または否認
- **備考**：
  - 値の修正は不可
  - 否認理由の記録はPoCでは対象外とする

### 自身の測定記録の閲覧
- **事前条件**承認済みの記録のみ閲覧可能
- **基本フロー**：
  1. 測定記録の閲覧専用ページを使用
  2. 承認済みの全測定記録を一覧で確認する（最大18回程度のため絞り込みなし）
- **備考**：値の修正は不可

### ■ 課題2

### 自身の測定記録のグラフ表示
- **事前条件**：承認済みの記録が1件以上存在する
- **基本フロー**：
  1. グラフ表示専用ページを使用
  2. 表示項目（項目混合・項目別など）を任意で選択しグラフを表示する
- **備考**：
  - 表示方法は項目別のみ（項目混合は対象外）
  - 表示方法の詳細はUI設計時に要検討

### その他拡張機能
- 承認依頼通知機能
- グラフの表示方法拡張（要検討）

---

## コーチ

---

### ■ 課題1

### 測定結果の最終承認
- **事前条件**：部員が承認済みデータのみ表示
- **基本フロー**：
  1. 測定結果の確認専用ページで対象日・各項目の値を確認
  2. 値に問題がない測定結果のみ個別に承認する（誤承認防止のため一括承認は不可）
- **備考**：誤承認防止対策について実装時に要検討

### 全部員の測定記録の閲覧
- **基本フロー**：
  1. 測定記録の閲覧専用ページを使用
  2. 承認済みの全測定記録を一覧で確認する
- **備考**：値の修正は不可

### 部員の作成／退部／引退
- **事前条件**：特になし
- **基本フロー**：
  1. 部員管理の専用画面を開く
  2. 同画面内の「部員作成」「退部処理」「引退処理」ボタンから各専用ページへ遷移する

  **部員作成**
  - 登録項目：メールアドレス・生徒名・学年・パスワード
  - パスワードはコーチ・監督が部員作成時に初期設定
  - 部員への初期パスワード通知は運用側で対応
  - セキュリティ上の割り切りとしてドキュメントに明記

  **退部処理**
  - 対象部員を名前・学年・メールアドレスなどで検索し選択後、退部ボタンを押す
  - 退部ボタン押下後、対象情報の確認画面を表示し最終確認を行う
  - データベース上、対象部員は退部部員としてソフトデリートで管理

  **引退処理**
  - 対象部員を名前・学年・メールアドレスなどで検索し選択後、引退ボタンを押す
  - 引退ボタン押下後、対象情報の確認画面を表示し最終確認を行う
  - データベース上、対象部員は引退部員としてソフトデリートで管理

- **備考**：特になし

### ■ 課題2

### 測定記録の検索・ソート
- 名前・学年・項目名での検索機能
- 各項目でのソート機能

### 測定記録のグラフ表示
- **事前条件**：承認済みの記録が1件以上存在する
- **基本フロー**：
  1. グラフ表示専用ページを使用
  2. 表示項目（項目混合・項目別など）を任意で選択しグラフを表示する
- **備考**：
  - 表示方法は項目別のみ（項目混合は対象外）
  - 表示方法の詳細はUI設計時に要検討

### その他拡張機能
- 承認依頼通知機能
- グラフの表示方法拡張（要検討）
- 3年生一括引退処理
- 承認履歴表示
- 承認コメント機能

---

## 監督

---

### ■ 課題1

### 全部員の測定記録の閲覧
- **備考**：コーチと同内容になります

### 部員の作成／退部／引退
- **備考**：コーチと同内容になります

### ■ 課題2
- **備考**：コーチと同内容になります

---

## セキュリティ上の割り切り（PoC）

| 項目 | 内容 |
|---|---|
| パスワード初期設定 | コーチ・監督が設定し運用側で部員に通知する（初回強制変更なし） |
| リフレッシュトークン | HttpOnly CookieのみでDB保存なし。即時失効不可 |
| MFA | 将来対応可能な設計とするが今回は未実装 |
| 否認理由 | 記録なし |